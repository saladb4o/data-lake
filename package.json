{
  "name": "data-lake-price-feeds",
  "private": true,
  "description": "Node dependencies for the price sources that have no HTTP API. Only TradingView needs one: it serves candles over a WebSocket session, not an endpoint requests can ask.",
  "scripts": {
    "candles": "node scripts/tradingview_candles.js"
  },
  "dependencies": {
    "@mathieuc/tradingview": "^3.4.2"
  }
}
