[pytest]
testpaths = tests
norecursedirs = Temp .agents node_modules gauntlet_out .git
python_files = test_*.py
markers =
    network: test reaches a live upstream API (vnstock, VNDirect, TradingView, RSS).
        Deselected in CI via -m "not network"; see tests/conftest.py.
